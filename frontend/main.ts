import { CanvasController } from './others/canvasNavigation.js';
import { Socket } from './others/socket.js';

const canvas = document.getElementById('canvas')!;
const canvasController = new CanvasController(canvas);

